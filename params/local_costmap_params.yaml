# local_costmap:
#   global_frame: odom
#   robot_base_frame: base_footprint
#   update_frequency: 5.0
#   publish_frequency: 2.0
#   static_map: false
#   rolling_window: true
#   width: 6.0
#   height: 6.0
#   resolution: 0.05
#   transform_tolerance: 0.5
#   inflation_radius: 0.55  # Radius for inflation of obstacles (adjust as needed)
#   obstacle_range: 3.0  # Maximum range at which obstacles are inserted into the costmap
#   observation_sources: laser_scan_sensor
#   laser_scan_sensor:
#     data_type: LaserScan
#     topic: scan  # Laser scan topic
#     marking: true
#     clearing: true
#   # plugins:
#   #   - name: obstacle_layer
#   #     type: "costmap_2d::ObstacleLayer"
#   #     enabled: true
#   #   - name: inflation_layer
#   #     type: "costmap_2d::InflationLayer"
#   #     enabled: true

global_frame: odom
rolling_window: true
width: 1.5
height: 1.5

plugins:
  - {name: obstacles_laser,           type: "costmap_2d::ObstacleLayer"}
  - {name: inflation_l,                 type: "costmap_2d::InflationLayer"}